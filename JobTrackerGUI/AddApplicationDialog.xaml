<Window x:Class="JobTrackerGUI.AddApplicationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Application"
        Height="250"
        Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Company:" Margin="0,0,10,10"/>
        <TextBox x:Name="CompanyBox"
                 Grid.Column="1"
                 Margin="0,0,0,10"/>

        <TextBlock Text="Position:"
                   Grid.Row="1"
                   Margin="0,0,10,10"/>
        <TextBox x:Name="PositionBox"
                 Grid.Row="1"
                 Grid.Column="1"
                 Margin="0,0,0,10"/>

        <TextBlock Text="Status:"
                   Grid.Row="2"
                   Margin="0,0,10,10"/>

        <ComboBox x:Name="StatusBox"
                  Grid.Row="2"
                  Grid.Column="1"
                  Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="Cancel"
                    Width="80"
                    Margin="0,10,10,0"
                    Click="Cancel_Click"/>

            <Button Content="Add"
                    Width="80"
                    Margin="0,10,0,0"
                    Click="Add_Click"/>
        </StackPanel>
    </Grid>
</Window>